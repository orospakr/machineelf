<h1>Listing players</h1>

<b>NB:  players need to be ADDED MANUALLY, because Ikariam pulled any means to see the logged in player's ID number.</b>

<table>
  <tr>
    <th>Ikariam</th>
    <th>Ikariam login</th>
  </tr>

<% for player in @players %>
  <tr>
    <td><%=h player.ikariam_id %></td>
    <td><%=h player.ikariam_login %></td>
    <td><%= link_to 'Show', player %></td>
    <td><%= link_to 'Edit', edit_player_path(player) %></td>
    <td><%= link_to 'Destroy', player, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New player', new_player_path %>
